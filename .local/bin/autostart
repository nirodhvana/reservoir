#!/bin/sh

autostart="remaps picom dunst unclutter sxhkd bluetoothd blueman-applet pipewire qpwgraph cupsd cups-browsed"

for program in $autostart; do
	pidof -sx "$program" || "$program" &
done >/dev/null 2>&1

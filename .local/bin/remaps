#!/bin/sh
# This script is called on startup to remap keys.
# Decrease key repeat delay and increase key repeat rate per second.
xset r rate 500 45
# Swap the function of caps lock & esc.
setxkbmap -option caps:swapescape
